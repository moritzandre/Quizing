# CLAUDE.md

Guidance for Claude Code when working in this repository.

## What this is

**Quiz Night** — a host-led party quiz web app (Vite + React 19 + Tailwind CSS v4). One host screen drives the game; players' phones can buzz in, drop map pins, pick multiple-choice answers and submit number guesses over a QR-code room. Fourteen round formats: `classic`, `jeopardy`, `hints` (hint ladder, where each hint can be text/image/audio/video/map), `video` (chrome-free YouTube, **Spotify**, or a **direct audio/video file**; optional audio-only; auto-pauses on first buzz), `clip` (clip ladder — the video/audio sibling of the hint ladder: play a short slice of a `[start,end]` window and extend it step by step via `steps` for fewer points each; same three sources — Spotify/files dodge YouTube's "embedding disabled" wall, common for music), `image` (picture), `morph` (progressive image reveal: blur/pixelate/tiles/zoom/slices), `fusion` (two images blended, defuse to reveal), `map` (real Leaflet map), `choice` (multiple-choice on phones, auto-scored), `truefalse` (Wahr/Falsch — binary auto-scored on phones), `higherlower` (Höher/Niedriger — binary auto-scored on phones), `number` (closest-guess on phones, auto-scored), `whoknows` (Who Knows More — a host-run **auction**: a player claims how many of a category they know, then must name at least that many against a per-answer clock or **bust**; host taps each correct answer and can showcase the full, optionally ranked, answer list). The `map` round is also **auto-scored** (closest pin wins). Playable solo or in **teams** (each team is one scoring entity; phones link to a team); players pick a **pixel-art character + colour** avatar on their phone (crisp 8-bit SVG sprites, identical on every device). A **TV presenter** view (`#/present/<code>`) streams a clean, read-only mirror of the host to a second screen over the same MQTT room (no HDMI; e.g. cast a Chrome tab to a Google TV), with an animated **podium-climb** live scoreboard and map pins hidden until reveal. A separate **host remote** (`#/host/<code>`) lets the host drive the whole game (reveal, advance, award, hints, …) from a phone over a `ctrl` command channel. The builder adds **templates** (per-round + whole-quiz starters), a **Creator Room** AI-authoring helper (copy a schema prompt, paste the JSON back) — at both whole-quiz (home) and per-round-type (builder) level — **JSON round import** (paste/upload a round, array, or quiz to append rounds to the one being edited), **image crop** (cropperjs), **video/audio trim** (start/end), and richer **maps** (Esri satellite layer + OSM Nominatim place search + an embedded **Mapillary street view** on map questions). Between rounds, a **recap** (with a fanfare) plays a random 8-bit minigame animating each entity's score from its start-of-round total to its end-of-round total, then **always finishes on a ranked leaderboard** (confetti + crowned winner) before the host advances (host + TV); live scoreboard numbers count up on award. Includes `.quiz.json` export/import, a playful light/dark theme, English/German UI (i18n), an optional per-round timer, a persistent leaderboard, and hash-based routing.

The app began as a single-file Claude.ai artifact (`quiz-night-app.tsx`, preserved in the first git commit), was refactored into modules, then extended with the features above.

## Commands

- `npm run dev` — dev server
- `npm run build` — production build (relative `base: "./"`; output in `dist/`)
- `npm test` — Vitest (unit tests next to the code, e.g. `src/lib/model.test.js`)
- `npm run lint` — ESLint (flat config, `eslint.config.js`)
- `npm run format` — Prettier (`printWidth: 120`)

Deploy: `.github/workflows/deploy.yml` publishes to GitHub Pages on push to `main`.

## Architecture

```
index.html                   Vite entry + pre-paint dark-theme bootstrap script
src/main.jsx                 entry; renders <Root> from App.jsx in StrictMode
src/App.jsx                  shell: hash routing (#/ #/play #/setup/<id> #/builder #/leaderboard #/join/<code> #/present/<code> #/host/<code>),
                             persistence, home (+ New-from-template & Creator-Room modals), owns the host buzzer room (useHostRoom) + leaderboard recording
src/index.css                Tailwind v4 import; class-based dark variant; `@theme` registers the 8-bit `font-pixel` family (Press Start 2P); keyframes (qn-hop/qn-climb-row/qn-score-pop/qn-float-up/qn-blink/qn-shake); Leaflet tooltip styling
src/lib/storage.js           storage adapter + loadJSON/saveJSON/removeKey/loadWithLegacy, SCHEMA_VERSION, STORAGE_PREFIX
src/lib/supabase.js          OPTIONAL shared-playerbase backend (framework-free). isSupabaseConfigured gate + lazy getSupabaseClient (dynamic-imports @supabase/supabase-js only when VITE_SUPABASE_* env is set); invisible anonymous auth + best-effort listPlayers/createPlayer/updatePlayer/verifyPin/setPin/recordResult/loadPlayerStats/loadGlobalLeaderboard (each null/[]/false on failure — never throws; PIN + result writes go through SECURITY DEFINER RPCs). Unconfigured = the SDK is dead-code-eliminated and the app behaves exactly as before.
src/lib/model.js             pure logic: utils, ytId, mediaSource (youtube/spotify/file), mapillaryEmbedUrl, moveItem, haversineKm, fileToDataUrl, factories (incl. makeHint),
                             normalizeQuiz/normalizeGame (round-trips an optional per-player profileId), roundsFromImport, hintHasContent, morphValue, clipLadderActive/clipEnd, summarizeGame/aggregateLeaderboard,
                             normalizeProfile/normalizePlayers/normalizeResults/summarizeResults/summarizeForProfile/normalizeLeaderboard/buildResultRow (playerbase + stats helpers, all pure/validated),
                             buildPresentQ/buildLive + normalizePresent/normalizeLive (TV payloads), buildHostAux/normalizeHostAux (host-remote-only aux: whoknows answer list), normalizePhoneScores (standings mirrored to phones), exportQuiz, ROUND_TYPES, BINARY_TYPES, RECAP_VARIANTS, MORPH_EFFECTS, HINT_TYPES
src/lib/realtime.js          MQTT-over-WebSocket room transport (public broker). Framework-free. connectRoom/roomTopics (state/up/present/live/host)/newRoomCode
src/i18n/strings.js          en/de string catalogs + translate(); LANGS. Framework-free.
src/i18n/I18nProvider.jsx    I18nContext + I18nProvider + useI18n() (lang/setLang/t) + LanguageToggle; persists quiznight.lang
src/data/sampleQuiz.js       built-in "Friday Night Sampler"
src/data/nerdQuiz.js         built-in "Nexus Nights" (LoL/nerd; demos morph/fusion/picture/media-hints/map/choice/number)
src/data/templates.js        ROUND_TEMPLATES (per-type starters) + QUIZ_TEMPLATES + AI_SCHEMA_HELP + roundCreatorPrompt(type) (per-type LLM prompt)
src/components/pixelSprites.js  8-bit avatar character grids (SPRITES/SPRITE_KEYS) + shade()/mirrorRow() (framework-free data)
src/components/pixelAvatar.jsx  PixelSprite — renders one sprite key as crisp <rect> SVG, tinted from the tile colour
src/components/ui.jsx        TYPES metadata + ACCENT/accentFor, optionsFor/binaryOptions (synthesize the fixed labels for truefalse/higherlower), PLAYER_COLORS + PLAYER_SPRITES/colorAt/spriteAt (PLAYER_EMOJI/emojiAt are back-compat aliases), Avatar (pixel sprite on a colour tile; the avatar's character is a SPRITE_KEYS key carried in the player `emoji` field), style constants
                             (FOCUS/inputCls/cardCls), Button/IconButton/TypeBadge/ConfirmDelete, useTheme/ThemeToggle, Confetti
src/components/useRoom.js    React hooks over realtime: useHostRoom (first-buzz arbiter, pin/answer collection, team lobby — a `leave` is deferred ~3s so a reload's re-`join` cancels the removal; optional `setRoomPass` join gate (admits only correct-passphrase devices, ignores buzz/pin/answer from non-admitted ones; the passphrase is never broadcast — only `needsPass`+admitted/rejected deviceId lists),
                             pushPresent/pushLive TV streaming + pushHost host-only aux + pushScores (standings → phones via the state topic), ctrl `command` from a host remote), usePlayerRoom (exposes state.scores),
                             usePresenterRoom(code, {host}) (TV mirror + sendCtrl for the host remote; host:true also reads the host-only topic → hostAux)
src/components/usePlayerbase.js React hook over lib/supabase.js: the SHARED player directory {configured, ready, players, current, currentId, select(player,pin?), create, refresh, isUnlocked, recordResult, statsFor, globalBoard}. Anon sign-in; caches the chosen player in `quiznight.player` + unlocked-locked-player ids in `quiznight.unlocked`. `configured:false` (no env) → ready immediately + caller uses the classic type-a-name flow.
src/components/StatsView.jsx phone page (#/me): the current player's personal stats (games/wins/best/total + recent) + the global all-time leaderboard (player_leaderboard view); standalone, graceful empty states.
src/components/LeafletMap.jsx real pan/zoom map; answer pin + colored guess markers + lines; onPick; light/dark + Esri satellite tiles; optional Nominatim search + Mapillary link-out
src/components/MapillaryEmbed.jsx embeds a Mapillary street-view (the /embed endpoint; keyless) for a map question's `street`
src/components/MorphImage.jsx stepped image reveal: blur / pixelate (canvas) / tiles / zoom / slices
src/components/FusionImage.jsx two images cross-faded; "defuse" swings the blend toward each; reveal shows both
src/components/HintMedia.jsx renders one hint by type (text/image/audio/video/map); audio/video honor a start/end trim
src/components/MediaPlayer.jsx source-aware clip dispatcher: picks YouTube/Spotify/file from the URL (lib/model mediaSource); one contract (start/end trim, audioOnly, `transport` {n,action} remote play/pause/restart, `controls` to hide the bar). Used by PlayView + RoundBody
src/components/YouTubePlayer.jsx chrome-free IFrame player; hides the title; audio-only cover; custom controls; start/end trim; one-shot onError recovery → embed-blocked fallback (Open on YouTube)
src/components/SpotifyPlayer.jsx Spotify embed (iFrame API) for clips YouTube blocks (esp. music); fully masked (title/cover hidden), our own controls; ~30s preview without Premium
src/components/NativeMediaPlayer.jsx native <audio>/<video> for direct file URLs; full precise trim/ladder, no embed limits, audio-only equalizer cover
src/components/ScoreBar.jsx  fixed scoreboard; +/- award toggle; tappable on reveal; scores count up (Avatar/AnimatedNumber from ui.jsx)
src/components/PodiumClimb.jsx animated live standings (FLIP-lite rows + hop on a gain); host overlay + TV; pure props
src/components/RoundRecap.jsx between-rounds recap **dispatcher**: reduced-motion → static RecapBoard; else → RecapShow with one of 5 random 8-bit minigame skins (the host chooses, mirrored to the TV via `live.recapVariant`+`recapRound`/`recapTotal` so both play the same one); animates each entity's score start→end using avatars as sprites; host + TV
src/components/recap/         recap show + minigames + helpers: **RecapShow.jsx** (the broadcast wrapper — owns the single rAF clock and stages intro "ROUND N" splash → minigame (passed a play-phase `progress` 0→1) → winner finale (crown + Confetti + sparkle); shows a live commentary caption from `recapStory`), recapShared.js (useRecapProgress rAF clock, scoreAt/glyphsForDelta/rankRecap, **phaseFor** per-entity stagger so the leader resolves last, **useRecapSfx** throttled per-event blips host-only), RecapBoard.jsx (static ranked fallback), SpaceInvadersRecap/RocketRaceRecap/BlockStackerRecap/PelletMuncherRecap/BrickBreakerRecap (one skin each; pure props {entities, present, progress}; scenery + motion + particles + per-event sfx). Commentary logic is pure in model.js (`recapStory`); event sounds (zap/shatter/chomp/thud/sparkle) in lib/sound.js; keyframes (qn-twinkle/drift/flicker/burst/squash/bob/pulse) in index.css
src/components/RoundBody.jsx read-only per-round renderer for the TV/host-phone (driven by present/live; no pins). `stage` decides if THIS screen plays the clip (renders MediaPlayer headless+transport) or shows a "plays elsewhere" placeholder — so only one screen makes sound. Media heights capped to fit one screen
src/components/PresenterView.jsx phone-less TV page (#/present/<code>): usePresenterRoom; renders RoundBody + PodiumClimb; when soundOnTv it's the audio stage — one-time tap-for-sound (autoplay), then plays the clip via live.transport
src/components/HostRemoteView.jsx phone host controller (#/host/<code>): present/live context (usePresenterRoom host:true → hostAux) + sendCtrl(reveal/advance/hint/media/skipRound/award/adjust/soundOnTv/…); give/take the round's points per player; Play/Pause/Restart for clips; embeds a compact RoundBody mirror (map/image; clips show a "plays on TV/PC" placeholder — the phone never makes sound); runs the whole **whoknows** auction from the phone (pick winner/claim → award → tap correct answers off the host-only answer list → bust/show-all/next)
src/components/PlayView.jsx  game flow: intro → question/board → end; timer; buzzer arm/lock; phone pins; keyboard;
                             streams present/live to the TV; "Stream to TV" modal + standings overlay
src/components/SetupView.jsx player/team entry; Solo/Teams toggle; embeds BuzzerPanel; seeds entities from the phone roster
src/components/Builder.jsx   quiz editor (all 10 types); SortableList drag-and-drop; ImageField + CropModal (cropperjs, lazy);
                             HintsField (typed hints + TrimInputs); morph/fusion/choice/number editors; Leaflet pin + satellite/search;
                             from-template insert + RoundImportModal (paste/upload JSON rounds, re-id'd) + RoundCreatorModal (per-type AI prompt → paste)
src/components/BuzzerPanel.jsx host lobby: QR (qrcode), room code, connected roster, status, + an optional **join passphrase** input (room.setRoomPass) — a no-login gate players must enter to join
src/components/JoinView.jsx  phone page (#/join/<code>): name + emoji/colour avatar + team picker → buzz / pin map / choice tap / number guess. When Supabase is configured (usePlayerbase), the pre-join step is a **shared player picker** (choose an existing player — PIN prompt if locked — or create one, optionally PIN-locked) and the join sends the chosen player's `profileId`; unconfigured it's the classic type-a-name flow. When the host set a join passphrase (`needsPass`), a pass field gates the join and a post-join screen blocks the game until the device is `admitted` (re-enter on `rejected`). At game end (state-topic `ended`) the phone writes its own stat row (`buildResultRow` → `record_result`). **Reconnect:** caches the last join (`quiznight.lastJoin`) and silently auto-rejoins a still-live room on reload (works with or without Supabase; persisted `deviceId` re-links the scoring entity); a "leave / switch player" button clears it. Gamified **8-bit** arcade HUD: each player sees its own live **score + rank** (from the state-topic `scores`, matched by `deviceId`), a "+N" coin-pop on every gain, and a between-questions leaderboard
src/components/LeaderboardView.jsx persistent standings table
src/components/ErrorBoundary.jsx render-error fallback
```

State model: `App` owns `quizzes`, `game`, `lastPlayers`, `leaderboard`, and the host `room` (useHostRoom); it persists game/quizzes/players/leaderboard on change. `PlayView` mutates the game purely through `setGame`. A game holds `id`, `mode` (`solo|teams`), `quiz` (deep copy), `players` (each `{id,name,score,color?,emoji?,deviceIds?,members?}` — `emoji` carries an 8-bit pixel-sprite KEY (a `SPRITE_KEYS` value, not an emoji char; legacy emoji/`photo` fields from old saves just fall back); `deviceIds` links one or more phones; in team mode each "player" is a team and `members` lists its phone names), `ri`/`qi`, `stage` (`intro|question|board|end`), `revealed`, `hintsShown`, `awarded` (reversible signed deltas), `used` (jeopardy tiles), `tile`, `guesses` (per-entity `{lat,lng}`), `roundStartScores` (per-entity totals snapshotted at the start of the current round, for the between-rounds recap). Round objects carry optional `timer` (seconds|null); video questions carry `audioOnly` and optional `start`/`end` (trim seconds), and auto-pause on first buzz via `pauseSignal`; clip questions carry the same fields plus `steps` (clip-ladder extensions, ≥1; the host plays the first `1/(steps+1)` of `[start,end]` and extends it, decaying points via `morphValue`); morph questions carry `effect` (one of `MORPH_EFFECTS`) and `steps`; fusion questions carry `urlA`/`urlB`/`steps`; choice questions carry `options[]`/`correct`; number questions carry `answer`/`unit`; whoknows questions carry `q` (the category prompt), `answers[]` (all correct answers — never sent to the TV until the host shows them) and `ordered` (display-only ranked list), with the round's `timer` reused as the per-answer clock; map questions carry optional `tileLayer` (`map|satellite`, default `map`) and `street` (a Mapillary share/embed URL or id, rendered via `mapillaryEmbedUrl`); hint-ladder hints are either plain strings (text) or typed objects (`{type:"image", url}`, `{type:"audio"|"video", url, start?, end?}`, or `{type:"map", lat, lng, name}`). Player entities carry an optional phone-chosen `color`/`emoji` (sprite key). The countdown timer and the morph/fusion step are UI-only (local `PlayView` state, the morph step reused for fusion and for the video clip-ladder step via `clipLadderActive`/`clipEnd` in model.js), not persisted. The whoknows auction state (`wk` = `{phase, winnerId, claimed, picked, result, showAll}` plus the per-answer `wkLeft` countdown) is likewise UI-only in `PlayView`; only the resulting score changes persist (deliver → winner +claimed; bust → every other entity +picked-so-far), and the TV mirrors it via the `live.whoknows` payload (`buildLive`/`normalizeWhoknows`), which omits the answer list until the host shows it. The per-answer clock **holds at zero rather than auto-busting** — the host stays in control and can still award one more answer (which regrants the clock) or end the turn deliberately. The host can run the entire auction from the **host remote** (the answer list to tap rides the host-only topic via `buildHostAux`/`hostAux`, never the TV channels).

Realtime: a scoring entity is a solo player or a team; `entityForDevice`/`mapByEntity` in `PlayView` re-key phone events (buzz/pins/answers, all keyed by `deviceId`) onto entities via `deviceIds`. For a team, the last phone to act wins. The host is the single arbiter of who buzzed first (no clock sync). Topics: `quiznight/<code>/state` (host→phones, retained; carries phase + `options`/`teams` + `scores` — the live standings each phone uses to show its own score/rank, validated by `normalizePhoneScores`; at game end also `ended`/`gameId`/`quizTitle`/`mode` so a phone can write its own stat row; when a join passphrase is set, `needsPass`+`admitted`/`rejected` deviceId lists — the passphrase itself is NEVER broadcast), `quiznight/<code>/up` (phones→host; join/buzz/pin/answer/leave), and the TV-presenter pair `quiznight/<code>/present` (heavy, per-question display payload) + `quiznight/<code>/live` (light, frequent: reveal/step/standings/showStandings + media `transport` {n,action} and `soundOnTv` + `recapVariant` — which random recap minigame the TV should mirror) — both retained, both built by `buildPresentQ`/`buildLive` and validated by `normalizePresent`/`normalizeLive`. The present/live payloads omit the answer and map coordinates until reveal, so the TV (`usePresenterRoom`) can never leak them and pins stay hidden until reveal. A fifth topic `quiznight/<code>/host` (host→host-remote ONLY, retained, `buildHostAux`/`normalizeHostAux`) carries reveal aids the TV must never see — currently the full **whoknows** answer list, so the host can tap each correct answer off their phone; `PresenterView` (the TV) never subscribes to it, only `usePresenterRoom(code,{host:true})` does (→ `hostAux`), so the TV invariant holds. The four retained topics (`state`/`present`/`live`/`host`) clear on host teardown; `up` is not retained (nothing to clear). A **host remote** phone (`#/host/<code>`, `usePresenterRoom.sendCtrl`) publishes `{type:"ctrl", action, args}` back on `up`; `useHostRoom` surfaces it as `command` (id-bumped so repeats re-fire) and `PlayView`'s applier effect dispatches it to the real handlers (reveal/advance/award/hint/sign/adjust/jump/skipRound/standings/resetBuzz/media/soundOnTv, plus the whoknows set wkWinner/wkClaim/wkAward/wkPick/wkBust/wkShowAll), guarding every untrusted arg (player-id membership, round-index bounds, answer-index bounds, transport action whitelist). For clips/video the audio plays on a single **stage** screen — the host PC by default, or the **TV** when `soundOnTv` is on (the TV taps once to satisfy autoplay, then follows `transport`); the other screens show a placeholder, so only one screen makes sound.

## Hard rules

- **`src/lib/` stays framework-free.** No React/JSX (storage.js, model.js, realtime.js qualify — they do I/O but import no React). React hooks that wrap them live in `src/components/` (e.g. useRoom.js).
- **Round-type metadata is split and must stay in sync:** `ROUND_TYPES` (keys, validation) in `lib/model.js`; `TYPES` (icon/dot, + English fallback label/desc) in `components/ui.jsx`; the localized label/desc are i18n keys `round.<type>.label` / `round.<type>.desc` in `i18n/strings.js`. Adding a type means updating all of those, plus `makeQuestion`/`normalizeQuiz`, the `PlayView` render branch, and the `Builder` editor branch. The binary auto-scored types `truefalse`/`higherlower` reuse the `choice` machinery (phone "choice" phase, `revealChoice`, live tally, RoundBody/PlayView choice branch) with two fixed options synthesized by `optionsFor` (ui.jsx) and an optional reveal `note`; their data is `{q, correct: 0|1, points, note}`.
- **All user-facing text goes through i18n.** Use `const { t } = useI18n()` and `t("key", vars)`; add the key to both `en` and `de` in `i18n/strings.js`. `translate()` falls back to English then the key, so a missing `de` entry degrades gracefully. Quiz CONTENT (questions/answers, built-in quizzes) is not translated.
- **Storage compatibility is load-bearing.** Keys `quiznight.quizzes`/`quiznight.game`/`quiznight.players`, the `{ v: 1, data }` envelope, the legacy migration in `loadWithLegacy`, and the three-backend adapter must keep working. New fields (`timer`, `audioOnly`, video/hint `start`/`end`, `clip` rounds with `steps` (clip ladder), map `tileLayer`/`street`, player `color`/`emoji` (now a pixel-sprite key), `image`/`morph`/`choice`/`number` rounds, `guesses`, `deviceIds`/`members`, game `mode`, `leaderboard`, `theme`, `deviceId`, player `profileId`) are additive/optional; old `.quiz.json` and saved data still load (a legacy single `deviceId` on a player migrates to `deviceIds`; the removed `photo` field and old emoji-char avatars are dropped/fall back). Theme is `quiznight.theme`, leaderboard is `quiznight.leaderboard`, phone identity is `quiznight.deviceId`, the chosen playerbase player is `quiznight.player` (+ unlocked-player ids in `quiznight.unlocked`), the last join (for reconnect) is `quiznight.lastJoin`. The TV present/live payloads are ephemeral (MQTT only), never persisted.
- **All untrusted data goes through `normalizeQuiz`/`normalizeGame`.** Never trust raw parsed JSON or incoming realtime messages without validating shapes. The realtime `join` message now also carries an optional `profileId` (validated in `useHostRoom`); Supabase rows go through `normalizeProfile`/`normalizeResults`.
- **The Supabase shared-playerbase backend is OPTIONAL and gated.** Everything routes through `isSupabaseConfigured` (true only when both `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` are set). With no env, the SDK is dead-code-eliminated and the app is byte-for-byte its old self (type-a-name each join, host-local name-keyed leaderboard). It may **never block or break a join** — every `lib/supabase.js` call is best-effort and returns `null`/`[]`/`false` on failure. `lib/supabase.js` stays framework-free (the SDK is dynamic-imported, lazy chunk); React lives in `usePlayerbase.js`. **Identity is a SHARED, login-free directory** (no email/accounts): players are picked or created on join; a player is free-to-claim or **numeric-PIN-locked** (whoever sets it first — no durable creator). Auth is an invisible anonymous session (decoupled from the player id); the anon key is publishable — **RLS + SECURITY DEFINER RPCs** (`supabase/migrations/0002_playerbase.sql`) are the boundary. The **PIN hash is never readable or writable by clients** (column `revoke` + a `profiles_public` view exposing only a `locked` boolean; `verify_pin`/`set_pin`); **`results` is write-locked** (insert only via the idempotent `record_result` RPC, immutable, world-readable for the global `player_leaderboard` board). PINs are a _soft lock_ (6–8 digits, bcrypt) with no recovery — not a security boundary. Phones write their OWN `results` rows at game end; the host-local leaderboard coexists (the only stats source when unconfigured / for phone-less manual players). Realtime stays on MQTT — Supabase is data-only. The `0002` migration ALTERs the live `0001` schema (drops the auth FK; self-only → shared RLS).
- **Tailwind classes stay inline in JSX.** The only CSS in `index.css` is the Tailwind import, the `dark` custom variant, keyframes, and third-party (Leaflet) tooltip styling.
- **Dark mode is class-based** (`.dark` on `<html>`, set pre-paint by the inline script in `index.html`, toggled by `useTheme`). Every surface needs paired `dark:` variants.
- **The buzzer + TV presenter use a free public MQTT broker** (`BROKER_URL` in realtime.js) — no backend, no account; a random room code is the only privacy boundary. Don't put anything sensitive on it. The dev/preview server binds to `0.0.0.0` (vite.config.js) so phones/TVs on the same Wi-Fi can reach it via the host's LAN IP; the QR encodes `window.location.origin`, so the host must open the app via that IP (not `localhost`) — BuzzerPanel shows a warning when it detects localhost. The TV opens that same origin at `#/present/<code>`.
- **A few other third parties are reached, all host/builder-only and benign:** map tiles from Carto (map) and Esri World Imagery (satellite); the builder map's place search hits **OpenStreetMap Nominatim** (sends the host's typed query, submit-driven, ~1 req/s) and the Mapillary link-out opens the current map centre. All tile sources are attributed via Leaflet's `attributionControl`. No player data leaves the device through these. **Clip/video media** loads from whichever source the author pastes: YouTube (IFrame API), **Spotify** (its embed iFrame API script + player — masked so the title can't leak), or a **direct audio/video file URL** (native element, fetched from wherever it's hosted). `mediaSource` (model.js) picks the player from the URL; the URL is the only thing stored/streamed. These run wherever a clip plays (host, TV, host remote) — keep each player lazy-friendly and never let a source leak the answer (Spotify's chrome stays covered).

## Conventions

- `lib/` and `data/`: named exports. Components: one per file, default export. Multi-export exceptions: `ui.jsx` (primitives + theme + confetti) and `useRoom.js` / `i18n/I18nProvider.jsx` (hooks + provider, exempted from the fast-refresh lint rule in `eslint.config.js`).
- JSDoc on exported functions.
- Conventional commit messages (`feat:`, `fix:`, `refactor(scope):`, …).
- Prettier + ESLint configured; run both before committing. Accepted lint warnings: `react-refresh/only-export-components` in `ui.jsx`. `useRoom.js` is exempted from that rule in `eslint.config.js`.
- New runtime deps beyond React/lucide-react: `mqtt` (buzzer/TV, dynamic-imported), `leaflet` (map), `qrcode` (join/TV QR), `cropperjs` (image crop, lazy-imported in `Builder`). Routes and the heavy libs are code-split, so the home bundle stays small; keep new heavy deps lazy.
- **`usePresenterRoom` is exempt from the fast-refresh lint rule** in `eslint.config.js` (it lives in `useRoom.js`, already exempted). Present/live payloads are built and validated only in `lib/model.js` (framework-free) so they stay unit-testable.
